spring:
  application:
    name: admin-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
